# Code style (shared)

Defaults across projects unless a repo overrides them.

- Types / type hints always.
- Comments only where the intent is non-obvious.
- Small, reviewable diffs over large rewrites.
- Match the existing style of the file you are editing.

Language-specific rules belong in a project's .claude/rules/.
